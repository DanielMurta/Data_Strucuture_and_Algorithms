class MyArray {

    constructor() {
        this.length = 0;
        this.data = {};
    }

    get(index) { //O(1)
    }

    push(item) { //O(1)
    }

    pop() { //O(1)
    }

    delete(index) { //O(n)
    }

    
}